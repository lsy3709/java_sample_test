Object : 최고 상위 클래스 
여기서 정의된 메서드들은 대부분 주솟값으로 작업을해서 
우리가 하는 작업과 맞지 않다. 
실제 데이터를 처리하는 작업을 많이해서 , 기존의 메서드들을 그대로 사용 안하고, 
재정의 사용한다. 

컬렉션 -> 여러 데이터(객체)를 담는 그릇. 
ArrayList -> 배열 업그레이드,순서있고, 중복 가능.
set-> 순서 없고, 중복 불가능.
Map -> key : value 형태로 저장. key 값을 기준으로 정렬 가능. 
Tree(부모노드, 자식노드 , 정렬: 오름차순, 내림차순) vs Hash (특정 값, 테이블로 저장)
!
제네릭 -> 담는 종류를 제한(특정 범위제한). <>
!

13 내부클래스-> 익명클래스(이름 없는 클래스 : 이름 없어서 호출이 안됨. 부르고
싶어도 못불러요. 그럼 어떻게 하냐? 1회성으로 사용을 합니다.
예) 이벤트 핸들러(버튼을 클릭했을 때 동작하는 작업, 자바 이후~계속 사용. )
, 람다식
(화살표 함수(메서드),메서드를 간단하게 정의해서 사용.) 오래전 부터 사용하고 있음. 
프로그래밍 기반이 다 수학.
 , 스트림
 (특정 (1) 데이터 선택, (2)정렬 , (3)출력) 기본적으로 사용되는 메서드들을 편하게
 모아 놓았다. 특정 기능의 즐겨찾기.(템플릿 메서드)
