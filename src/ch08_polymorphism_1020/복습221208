복습.

클래스 -> 객체 생성 -> 
VipCustomer extends Customer // 상속 관계 , 부모 클래스 , 자식 클래스 
// 자식이 부모꺼를 사용할려면, 먼저는 부모에 있는 것부터 초기화를 해야한다. (생성자 호출 : super())
Customer lsy;
lsy = new Customer(); // 0x100
VipCustomer lsy2 = new VipCustomer();
!
배열, ArrayList 
여러 데이터를 담는 그릇 : 배열 , 담을수 있는 크기가 고정.
반면에, 
ArrayList : 여러 데이터를 담는 그릇 :  , 담을수 있는 크기가 고정 아님. 가변
기본형 vs 참조형( 변수에 주솟값을 담는다.)
!
제너릭 -> <Customer> : Customer 이 형만 담을수 가 있다. 
!
protected : 외부에서 안보임. 자식에서만 보인다.(상속 관계).
!
다형성 

VipCustomer lsy2 = new VipCustomer();
Customer lsy3 = new VipCustomer();
!
lsy3. : 어디가 보이죠? Customer 꺼 보입니다.
!
가상 메서드 .!
오버로드 : 짐 ( 매개변수) , 생성자, 메서드의 이름은 같지만, 매개변수 의 형식이 달랐다. 각각 다르게 취급. 
오버라이드 : 덮어쓰기, 재정의. 메서드 이름은 같지만, 안에 기능을 다르게 정의해서 사용한다. 
!